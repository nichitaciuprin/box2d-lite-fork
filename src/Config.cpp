#include "MathUtils.h"

namespace Config
{
    Vec2 gravity = { 0, -10.0f };
    int iterations = 10;
    bool accumulateImpulses = true;
    bool warmStarting = true;
    bool positionCorrection = true;
}
